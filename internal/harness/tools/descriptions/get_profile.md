Get the full details of a specific agent profile by name.

Returns the profile's metadata (name, description, version), runner configuration (model, max_steps, max_cost_usd), allowed tools list, and source_tier (where the profile was resolved from: "project", "user", or "built-in").

Use list_profiles to discover available profile names before calling this tool.
