Inspect the resolved tool manifest for a named profile.

Returns:
- The profile source tier
- The declared tool allowlist from the profile
- The visible tools offered by default
- The deferred tools the profile can still reach
- Tier and source metadata for each resolved tool entry

Use this to verify what a profile-backed run can access before delegating work.
